Function Main()
    Local nCont  := 0
    Local nGraos  := 1
    Local Ntotal := 0

    for nCont := 1 to 64
        nTotal += nGraos
        nGraos := nGraos * 2

    qout("quadrado: " + alltrim(str(nCont) + " numero de graos: " +alltrim(str(nTotal))))
    NEXT

    
RETURN NIL
